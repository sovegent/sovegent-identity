{
  "name": "@sovegent/cli",
  "version": "0.1.0",
  "description": "Sovegent Identity command-line tool — keygen, attest, notarize, anchor, and ZK proofs",
  "license": "AGPL-3.0-or-later",
  "type": "module",
  "bin": {
    "sovegent-identity": "./dist/cli.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@sovegent/anchors": "workspace:*",
    "@sovegent/core": "workspace:*",
    "@sovegent/sdk": "workspace:*",
    "@sovegent/zk": "workspace:*",
    "@noble/curves": "^1.4.0",
    "@noble/hashes": "^1.4.0"
  },
  "devDependencies": {
    "typescript": "^7.0.2",
    "@types/node": "^20.0.0",
    "vitest": "^1.6.0"
  }
}
